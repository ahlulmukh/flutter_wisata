import 'package:flutter/material.dart';

class VerificationStorePage extends StatelessWidget {
  const VerificationStorePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Anda belum verifikasi')),
    );
  }
}
